icon: https://oomol-flows.oss-cn-hangzhou.aliyuncs.com/1723880439_matplotlib.png
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: pip install pandas
  bootstrap: |
    pnpm install --force && pip install -r requirements.txt
  dependencies:
    tools: 0.0.3
dependencies:
  read-file: 0.0.1
name: data-analysis-examples
version: 0.0.4
displayName: Data Analysis Examples
description: Shows previews of graphs and tables in workflows
releaseNotes: add code repo
repository: https://github.com/oomol-flows/data-analysis-examples
